{
  "database": "template",
  "method": "POST",
  "url": "/api/fields/article",
  "data": {
    "field": "categories",
    "type": "alias",
    "meta": {
      "special": [
        "m2m"
      ],
      "interface": "list-m2m",
      "options": {
        "template": "{{article_category_id.name}}"
      },
      "display": "related-values",
      "display_options": {
        "template": "{{article_category_id.name}}"
      }
    },
    "collection": "article"
  }
}